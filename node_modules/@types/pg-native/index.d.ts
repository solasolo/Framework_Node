
export default class PgConnetion {
    constructor();
    connect(opts: any, callback: AsnycCallback): void;
    query(sql: string, params: any[], callback: AsnycCallback): void;
    end(): void;
    on(event: string, callback: ErrorHandler): void;
}