import { BanchTest } from 'Performance';
import { DateTime } from 'DateTime';


let d = new Date();
let s;

BanchTest("format", () => {
    s = DateTime.Format("yyyyMMddhhmmss", d);
});
console.log(s);

BanchTest("compant", () => {
    s = DateTime.CompactDate(d);
});
console.log(s);

console.log(d.toISOString());