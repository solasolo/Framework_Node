#!/store/node/bin/node

import http from 'http';
import fs from 'fs';
import URLPaeser from 'url';
import crypto from 'crypto';

const options = {
    //key: fs.readFileSync('/etc/letsencrypt/live/www.t1hd.cn/privkey.pem'),
    //cert: fs.readFileSync('/etc/letsencrypt/live/www.t1hd.cn/fullchain.pem')
};

http.createServer((req, res) => {
    console.log(req.url);

    let parts = URLPaeser.parse(req.url || "", true);
    let Queries = parts.query;


    //console.log(SignatureCheck);
    //console.log(Checked);    

    
    res.writeHead(200);
    // res.end(echostr);
  
}).listen(8090);