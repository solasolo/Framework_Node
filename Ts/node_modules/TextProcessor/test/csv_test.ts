import { CSVReader } from "../lib/CSVReader";

const csv = new CSVReader;

async function test() {
    const iter = await csv.Read("d:\\Work\\T1HD\\Data\\跨境入仓6.csv");

    for (let d of iter) {
        console.log(d);
    }
}

test().then();