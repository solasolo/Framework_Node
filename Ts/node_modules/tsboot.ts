const path = require('path');

const g: any = global;

function Patch(dir?: string) {
    let ModulePath = dir ? dir + '/node_modules' : __dirname;

    process.env.NODE_PATH = process.env.NODE_PATH + path.delimiter + ModulePath;
    require('module').Module._initPaths();
}

function TSBoot() {
    g.__importDefault = (mod: any) => {
        return (mod && mod.__esModule) ? mod : {
            "default": mod
        };
    };

    g.__exportStar = function (m: string, exports: any) {
        // for (var p in m) if (p !== "default" && p !== "__esModule" && !exports.hasOwnProperty(p)) __createBinding(exports, m, p);
    };
}

TSBoot();

export = null;