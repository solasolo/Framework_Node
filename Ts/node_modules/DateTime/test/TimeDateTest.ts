import { DateTime as d } from '../lib/DateTime';

const log = console.log;

log(d.CompactDate());
log(d.LocalCompactDate());
log(d.Milliseconds());
log(d.LocalMilliseconds());
log(d.TimeStamp());
log(d.LocalTimestamp());
log(d.LocalISODate());
log(d.StandardFormat(new Date()));
log(d.LocalTime());
log(d.StandardFormat(d.LocalTime()));